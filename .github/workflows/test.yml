on:
  workflow_dispatch:
    inputs:
      URL:
        description: 'https://accessibilitydemo.teamtailor.app'
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test action
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test action
        uses: ./
