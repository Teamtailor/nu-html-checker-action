on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test action
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test action
        uses: ./
        with:
          url: https://example.com
